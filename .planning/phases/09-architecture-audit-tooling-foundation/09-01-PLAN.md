---
phase: 09-architecture-audit-tooling-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - packages/core/package.json
  - packages/core/src/lib/types.ts
autonomous: true
requirements: [ARCH-01, ARCH-03, ARCH-04]

must_haves:
  truths:
    - "Developer reading PROJECT.md KEY DECISIONS table finds a row explaining why src/clipboard/ is flat, with a forward-looking rule"
    - "size-limit check passes with 1.5KB budget for both dist/index.mjs and dist/clipboard/index.mjs"
    - "RICH_CLIPBOARD_NOT_SUPPORTED is a valid member of the ErrorCode union and can be imported"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Flat clipboard structure rationale in KEY DECISIONS table"
      contains: "src/clipboard/ flat structure"
    - path: "packages/core/package.json"
      provides: "Updated size-limit budgets"
      contains: "1.5 KB"
    - path: "packages/core/src/lib/types.ts"
      provides: "RICH_CLIPBOARD_NOT_SUPPORTED error code"
      contains: "RICH_CLIPBOARD_NOT_SUPPORTED"
  key_links:
    - from: "packages/core/src/lib/types.ts"
      to: "ErrorCode union"
      via: "type union member"
      pattern: "RICH_CLIPBOARD_NOT_SUPPORTED"
---

<objective>
Document the flat clipboard structure rationale, raise size-limit budgets for rich clipboard era, and add the RICH_CLIPBOARD_NOT_SUPPORTED error code.

Purpose: Prepare the codebase foundation (docs, config, types) so Phase 10 rich clipboard work lands cleanly without needing structural debates or config changes mid-feature.
Output: Updated PROJECT.md with KEY DECISIONS entry, updated size-limit config in package.json, new error code in types.ts.
</objective>

<execution_context>
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/workflows/execute-plan.md
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/lib/types.ts
@packages/core/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document flat clipboard structure in KEY DECISIONS and add error code</name>
  <files>.planning/PROJECT.md, packages/core/src/lib/types.ts</files>
  <read_first>
    - .planning/PROJECT.md (current KEY DECISIONS table format)
    - packages/core/src/lib/types.ts (current ErrorCode union pattern)
  </read_first>
  <action>
**PROJECT.md (per D-04, D-05 implementing ARCH-01):**

Add a new row to the KEY DECISIONS table in `.planning/PROJECT.md`. Insert it after the last existing row (before the `## Evolution` section). The new row:

| `src/clipboard/` flat structure | Discoverability -- all clipboard functions live at the same level; consumers see a flat, predictable surface without needing to know an internal rich/ vs plain/ split. Forward rule: new clipboard functions go flat into `src/clipboard/` until an explicit architectural decision is made to reorganize. | Active -- enforced from Phase 9 onward |

**types.ts (per D-09, D-10 implementing ARCH-04):**

In `packages/core/src/lib/types.ts`, add `'RICH_CLIPBOARD_NOT_SUPPORTED'` as a new union member to the `ErrorCode` type. Place it after the last existing member (`'INSECURE_CONTEXT'`). The resulting type should be:

```typescript
export type ErrorCode =
	| 'CLIPBOARD_NOT_SUPPORTED'
	| 'CLIPBOARD_PERMISSION_DENIED'
	| 'CLIPBOARD_WRITE_FAILED'
	| 'CLIPBOARD_READ_FAILED'
	| 'INSECURE_CONTEXT'
	| 'RICH_CLIPBOARD_NOT_SUPPORTED'
```

No additional JSDoc needed beyond the existing type comment (per D-10).
  </action>
  <verify>
    <automated>grep -q "flat structure" .planning/PROJECT.md && grep -q "RICH_CLIPBOARD_NOT_SUPPORTED" packages/core/src/lib/types.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md KEY DECISIONS table contains a row with "src/clipboard/ flat structure" in the Decision column
    - .planning/PROJECT.md KEY DECISIONS table row contains "Discoverability" in the Rationale column
    - .planning/PROJECT.md KEY DECISIONS table row contains "new clipboard functions go flat" forward-looking rule
    - packages/core/src/lib/types.ts contains `| 'RICH_CLIPBOARD_NOT_SUPPORTED'` as a union member of ErrorCode
    - ErrorCode union now has exactly 6 members (5 existing + 1 new)
  </acceptance_criteria>
  <done>PROJECT.md has flat structure rationale with forward rule in KEY DECISIONS. ErrorCode union includes RICH_CLIPBOARD_NOT_SUPPORTED.</done>
</task>

<task type="auto">
  <name>Task 2: Raise size-limit budgets to 1.5KB</name>
  <files>packages/core/package.json</files>
  <read_first>
    - packages/core/package.json (current size-limit entries)
  </read_first>
  <action>
**package.json (per D-06, D-07 implementing ARCH-03):**

In `packages/core/package.json`, update the `"size-limit"` array. Change BOTH entries from `"1 KB"` to `"1.5 KB"`:

```json
"size-limit": [
  {
    "path": "dist/index.mjs",
    "limit": "1.5 KB"
  },
  {
    "path": "dist/clipboard/index.mjs",
    "limit": "1.5 KB"
  }
]
```

After editing, run `pnpm --filter=@ngockhoi96/ctc build && pnpm --filter=@ngockhoi96/ctc size` to confirm the size check passes with the new 1.5KB budget.
  </action>
  <verify>
    <automated>cd /home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc && pnpm --filter=@ngockhoi96/ctc build && pnpm --filter=@ngockhoi96/ctc size</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/package.json `"size-limit"` array first entry has `"limit": "1.5 KB"` for `dist/index.mjs`
    - packages/core/package.json `"size-limit"` array second entry has `"limit": "1.5 KB"` for `dist/clipboard/index.mjs`
    - `pnpm --filter=@ngockhoi96/ctc size` exits with code 0 (passes)
    - No other fields in package.json are modified
  </acceptance_criteria>
  <done>Both size-limit entries raised to 1.5KB and size check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies docs, a type definition, and a config value -- no runtime trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | packages/core/package.json | accept | size-limit is a dev-only config; CI validates it on every PR; no production impact |
| T-09-02 | Information Disclosure | .planning/PROJECT.md | accept | Planning docs are public repo content; no secrets or PII |
</threat_model>

<verification>
- `grep "flat structure" .planning/PROJECT.md` returns the KEY DECISIONS row
- `grep "RICH_CLIPBOARD_NOT_SUPPORTED" packages/core/src/lib/types.ts` returns the union member
- `pnpm --filter=@ngockhoi96/ctc size` passes with 0 exit code
- `pnpm --filter=@ngockhoi96/ctc build` succeeds (types compile)
</verification>

<success_criteria>
1. PROJECT.md KEY DECISIONS table has flat structure rationale with forward-looking rule
2. size-limit budgets are 1.5KB for both entries and size check passes
3. RICH_CLIPBOARD_NOT_SUPPORTED exists in ErrorCode union
4. Build succeeds with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-architecture-audit-tooling-foundation/09-01-SUMMARY.md`
</output>
