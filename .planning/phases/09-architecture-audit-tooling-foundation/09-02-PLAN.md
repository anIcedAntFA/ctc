---
phase: 09-architecture-audit-tooling-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "CI runs a dedicated validate job that is separate from the unit-test job"
    - "validate job runs parallel to unit-test and e2e-test, all three depending on lint-and-build"
    - "validate job runs publint + attw on all 4 packages (core, react, vue, svelte)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Dedicated validate CI job"
      contains: "validate:"
  key_links:
    - from: ".github/workflows/ci.yml validate job"
      to: "lint-and-build job"
      via: "needs: lint-and-build"
      pattern: "needs:\\s*lint-and-build"
---

<objective>
Extract the validate step (publint + attw) from the unit-test job into a dedicated validate CI job that runs parallel to unit-test and e2e-test.

Purpose: CI validation (package correctness) should not be buried inside the test matrix. A dedicated job makes failures immediately visible and runs faster (no Node version matrix needed for validation).
Output: Updated ci.yml with a new validate job and the validate step removed from unit-test.
</objective>

<execution_context>
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/workflows/execute-plan.md
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/ci.yml

<interfaces>
<!-- Current CI job structure -->
Jobs in ci.yml:
  1. lint-and-build: checkout, pnpm, setup-node@22, install, lint, build
  2. unit-test: needs lint-and-build, matrix [20, 22], checkout, pnpm, setup-node, install, test --coverage, validate (TO BE REMOVED)
  3. e2e-test: needs lint-and-build, checkout, pnpm, setup-node@22, install, playwright, build, test:e2e

Turbo pipeline (turbo.json):
  validate: { dependsOn: ["build"], inputs: ["dist/**", "package.json"], outputs: [] }

All 4 packages already have validate scripts:
  - core: "publint && attw --pack"
  - react: "publint && attw --pack"
  - vue: "publint && attw --pack"
  - svelte: "publint && attw --pack --profile node16"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract validate into dedicated CI job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current job structure, step patterns, action versions)
    - turbo.json (validate task definition)
  </read_first>
  <action>
**Per D-01, D-02, D-03 implementing ARCH-02:**

Modify `.github/workflows/ci.yml` with two changes:

**Change 1: Remove validate step from unit-test job.**

Delete these two lines from the `unit-test` job:

```yaml
      - name: Validate package (publint + attw)
        run: pnpm turbo run validate
```

**Change 2: Add new validate job.**

Insert a new `validate` job between the `unit-test` and `e2e-test` jobs. The job:
- Name: `Validate Packages (publint + attw)`
- `needs: lint-and-build` (per D-02: parallel to unit-test and e2e-test)
- Runs on `ubuntu-latest`
- Single Node version 22 (no matrix -- validation is not Node-version-sensitive, per Claude's Discretion)
- Steps follow the exact same checkout/pnpm/setup-node pattern as other jobs
- Runs `pnpm turbo run validate` which will validate all 4 packages via turbo

The exact YAML for the new job:

```yaml
  validate:
    name: Validate Packages (publint + attw)
    needs: lint-and-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build (needed for dist/ validation)
        run: pnpm turbo run build

      - name: Validate packages
        run: pnpm turbo run validate
```

Also update the file header comment from:
```
# Pipeline: lint-and-build -> unit-test (matrix) -> e2e-test
```
to:
```
# Pipeline: lint-and-build -> [unit-test (matrix) | validate | e2e-test]
```

The final job order in the file should be: lint-and-build, unit-test, validate, e2e-test.
  </action>
  <verify>
    <automated>cd /home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc && grep -q "validate:" .github/workflows/ci.yml && grep -q "Validate Packages" .github/workflows/ci.yml && ! grep -A1 "Validate package (publint" .github/workflows/ci.yml | grep -q "pnpm turbo run validate" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains a job named `validate:` (top-level job key)
    - validate job has `name: Validate Packages (publint + attw)`
    - validate job has `needs: lint-and-build`
    - validate job has `runs-on: ubuntu-latest`
    - validate job has `node-version: 22`
    - validate job has step `run: pnpm turbo run validate`
    - validate job has step `run: pnpm turbo run build` (needs dist/ for attw)
    - unit-test job does NOT contain `Validate package (publint + attw)` step
    - unit-test job does NOT contain `pnpm turbo run validate` anywhere
    - File header comment mentions `[unit-test (matrix) | validate | e2e-test]`
    - YAML is valid (no syntax errors)
  </acceptance_criteria>
  <done>CI has a dedicated validate job running parallel to unit-test and e2e-test, and the validate step is removed from unit-test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner | GitHub Actions runners execute untrusted PR code; validate job runs publint/attw which inspect dist/ artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | .github/workflows/ci.yml | accept | CI YAML changes require PR review; no secrets used in validate job; publint+attw are read-only analysis tools |
| T-09-04 | Elevation of Privilege | validate job | accept | Job uses same checkout/install pattern as existing jobs; no elevated permissions granted; no `permissions:` block needed |
</threat_model>

<verification>
- `grep "validate:" .github/workflows/ci.yml` returns the job key
- `grep "needs: lint-and-build" .github/workflows/ci.yml` matches validate job
- unit-test job no longer has a validate step
- YAML syntax valid (GitHub Actions will parse it)
</verification>

<success_criteria>
1. validate job exists as a dedicated CI job in ci.yml
2. validate job depends on lint-and-build and runs parallel to unit-test and e2e-test
3. validate job runs pnpm turbo run validate (covering all 4 packages)
4. unit-test job no longer runs validation
</success_criteria>

<output>
After completion, create `.planning/phases/09-architecture-audit-tooling-foundation/09-02-SUMMARY.md`
</output>
