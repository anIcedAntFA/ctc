---
phase: "13-documentation-positioning-overhaul-update-root-and-package-r"
plan: "04"
type: execute
wave: 3
depends_on: []
files_modified:
  - BENCHMARKS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "BENCHMARKS.md Bundle Size Comparison table has 9 columns: Package, Version, Raw, Gzip, Brotli, API Style, Last Updated, TypeScript, SSR-safe (D-08)"
    - "The table includes three new library rows: react-copy-to-clipboard, usehooks-ts, @vueuse/core (D-09)"
    - "Existing rows for @ngockhoi96/ctc, clipboard-copy, copy-to-clipboard retain their existing size measurements and gain the four new column values"
    - "The Core Function Performance and React Adapter Overhead sections remain unchanged"
  artifacts:
    - path: "BENCHMARKS.md"
      provides: "Expanded bundle size comparison table"
      contains: "API Style"
  key_links:
    - from: "README.md 'Why ctc?' narrative"
      to: "BENCHMARKS.md expanded table"
      via: "link [See full comparison →](./BENCHMARKS.md)"
      pattern: "Bundle Size Comparison"
---

<objective>
Expand the BENCHMARKS.md Bundle Size Comparison table with four new columns (API Style, Last Updated, TypeScript, SSR-safe) and three new library rows (react-copy-to-clipboard, usehooks-ts, @vueuse/core).

Purpose: Give developers a comprehensive comparison view so they can see how ctc's modern API, TypeScript support, and SSR-safety compare to commonly used alternatives. Per D-08 and D-09.
Output: Updated BENCHMARKS.md with expanded table. Performance sections (Core Function Performance, React Adapter Overhead) are untouched.
</objective>

<execution_context>
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/workflows/execute-plan.md
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-documentation-positioning-overhaul-update-root-and-package-r/13-CONTEXT.md
@.planning/phases/13-documentation-positioning-overhaul-update-root-and-package-r/13-RESEARCH.md
@.planning/phases/13-documentation-positioning-overhaul-update-root-and-package-r/13-PATTERNS.md
@BENCHMARKS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Bundle Size Comparison table with new columns and new library rows</name>
  <files>BENCHMARKS.md</files>
  <read_first>
    - BENCHMARKS.md (read entire file before editing — must see exact current table format and line structure)
    - .planning/phases/13-documentation-positioning-overhaul-update-root-and-package-r/13-RESEARCH.md (Competitor Library Data section for verified facts)
    - .planning/phases/13-documentation-positioning-overhaul-update-root-and-package-r/13-PATTERNS.md (BENCHMARKS.md pattern section for exact column names and row values)
  </read_first>
  <action>
    Replace the entire `## Bundle Size Comparison` section (lines 5–11 of the current file) with the expanded version below.

    The current section:
    ```markdown
    ## Bundle Size Comparison

    | Package | Version | Raw | Gzip | Brotli |
    |---------|---------|-----|------|--------|
    | @ngockhoi96/ctc | 0.2.2 | 4.52 KB | 1.17 KB | 0.99 KB |
    | clipboard-copy | 4.0.1 | 0.93 KB | 0.53 KB | 0.43 KB |
    | copy-to-clipboard | 3.3.3 | 2.23 KB | 1.06 KB | 0.88 KB |
    ```

    Replace with:
    ```markdown
    ## Bundle Size Comparison

    | Package | Version | Raw | Gzip | Brotli | API Style | Last Updated | TypeScript | SSR-safe |
    |---------|---------|-----|------|--------|-----------|--------------|------------|----------|
    | @ngockhoi96/ctc | 0.2.2 | 4.52 KB | 1.17 KB | 0.99 KB | Function / Hook / Composable / Action | Active | Native (strict) | Yes |
    | clipboard-copy | 4.0.1 | 0.93 KB | 0.53 KB | 0.43 KB | Function call | 2020-11-18 | No | No |
    | copy-to-clipboard | 3.3.3 | 2.23 KB | 1.06 KB | 0.88 KB | Function call | 2022-11-13 | Bundled index.d.ts | No (execCommand) |
    | react-copy-to-clipboard | 5.1.1 | — | — | — | Render-prop component | 2026-03-07 | @types pkg | No |
    | usehooks-ts (useClipboard) | 3.1.1 | — | — | — | React hook | 2025-02-05 | Native | Partial |
    | @vueuse/core (useClipboard) | 14.2.1 | — | — | — | Vue composable | 2026-02-10 | Native | Partial |
    ```

    Notes on the new rows:
    - `react-copy-to-clipboard`, `usehooks-ts`, `@vueuse/core`: bundle size cells use `—` (em dash) because these are framework-specific packages whose standalone size is not directly comparable to the framework-agnostic ctc core. This is honest and more informative than running a potentially misleading tree-shaken measurement.
    - "Last Updated" values are exact npm publish dates from RESEARCH.md (verified 2026-04-17).
    - "TypeScript" column values:
      - `@ngockhoi96/ctc`: `Native (strict)` — TypeScript-first, no `any`
      - `clipboard-copy`: `No` — no bundled types, no `@types` package (verified in RESEARCH.md)
      - `copy-to-clipboard`: `Bundled index.d.ts` — ships types but not TypeScript-native
      - `react-copy-to-clipboard`: `@types pkg` — via `@types/react-copy-to-clipboard@5.0.7`
      - `usehooks-ts`: `Native` — TypeScript-native multi-utility package
      - `@vueuse/core`: `Native` — TypeScript-native multi-utility package
    - "SSR-safe" column:
      - `@ngockhoi96/ctc`: `Yes` — all functions guard with `typeof navigator !== 'undefined'`
      - `clipboard-copy`: `No` — accesses DOM directly without guards
      - `copy-to-clipboard`: `No (execCommand)` — uses `document.execCommand` which requires DOM
      - `react-copy-to-clipboard`: `No` — wraps `copy-to-clipboard` which uses execCommand
      - `usehooks-ts`: `Partial` — some SSR guards, depends on hook usage context
      - `@vueuse/core`: `Partial` — SSR guards per-composable, depends on usage

    Preserve everything after the table unchanged:
    - Blank line after table
    - `## Core Function Performance` section
    - `## React Adapter Overhead` section
    - `## Methodology` section
  </action>
  <verify>
    <automated>grep -n "API Style\|Last Updated\|TypeScript\|SSR-safe\|react-copy-to-clipboard\|usehooks-ts\|vueuse" /home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/BENCHMARKS.md</automated>
  </verify>
  <done>
    - Grep finds `API Style`, `Last Updated`, `TypeScript`, `SSR-safe` in the table header line
    - Grep finds `react-copy-to-clipboard` row
    - Grep finds `usehooks-ts` row
    - Grep finds `@vueuse/core` or `vueuse` row
    - Original three rows for ctc, clipboard-copy, copy-to-clipboard still present with their existing size values
    - `## Core Function Performance` section still present (not accidentally deleted)
    - `## Methodology` section still present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BENCHMARKS.md competitor data → reader | Competitor data must be accurate and sourced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | Competitor library facts | mitigate | All facts sourced from RESEARCH.md which has "HIGH confidence" verification against npm registry. Em dash used for framework-specific bundle sizes rather than invented measurements. |
</threat_model>

<verification>
```bash
grep -c "|" /home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/BENCHMARKS.md
```
Should return at least 10 (header row + separator row + 6 data rows + section dividers).

```bash
grep -n "^##" /home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/BENCHMARKS.md
```
Should show all four sections still present: Bundle Size Comparison, Core Function Performance, React Adapter Overhead, Methodology.
</verification>

<success_criteria>
- Bundle Size Comparison table header has 9 columns (Package, Version, Raw, Gzip, Brotli, API Style, Last Updated, TypeScript, SSR-safe)
- Table has 6 data rows: @ngockhoi96/ctc, clipboard-copy, copy-to-clipboard, react-copy-to-clipboard, usehooks-ts, @vueuse/core
- Existing size measurements (4.52 KB, 0.93 KB, 2.23 KB) are preserved in the original three rows
- Core Function Performance and React Adapter Overhead sections are unchanged
- Methodology section is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/13-documentation-positioning-overhaul-update-root-and-package-r/13-04-SUMMARY.md`
</output>
