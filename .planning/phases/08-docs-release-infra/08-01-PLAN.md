---
phase: 08-docs-release-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/core/README.md
autonomous: true
requirements:
  - DX-05
  - DX-06
user_setup: []
must_haves:
  truths:
    - "A developer landing on the GitHub repo sees a hub-style root README with a package table listing all four packages and one quick-start snippet"
    - "A developer clicking through to packages/core sees a full API reference README with every exported function, options type, and error code documented"
    - "Root README no longer duplicates core API detail — API documentation has a single source of truth at packages/core/README.md"
    - "Root README still shows existing badges (npm version, bundle size, CI, license)"
    - "Root README still contains the browser support table and the SSR-safe callout"
  artifacts:
    - path: "README.md"
      provides: "Monorepo hub README with package table, minimal quick-start, monorepo structure, browser support"
      contains: "## Packages"
    - path: "packages/core/README.md"
      provides: "Core package full API reference extracted from current root README"
      contains: "## API Reference"
  key_links:
    - from: "README.md"
      to: "packages/core/README.md"
      via: "markdown link in package table row"
      pattern: "\\./packages/core/README\\.md"
    - from: "README.md"
      to: "packages/react/README.md"
      via: "markdown link in package table row"
      pattern: "\\./packages/react/README\\.md"
    - from: "README.md"
      to: "packages/vue/README.md"
      via: "markdown link in package table row"
      pattern: "\\./packages/vue/README\\.md"
    - from: "README.md"
      to: "packages/svelte/README.md"
      via: "markdown link in package table row"
      pattern: "\\./packages/svelte/README\\.md"
---

<objective>
Rewrite the root `README.md` as a monorepo hub/landing page and create `packages/core/README.md` containing the detailed API reference extracted from the current root README. This satisfies DX-05 (root README reflects monorepo structure with links to each package) and the `packages/core` portion of DX-06 (per-package README).

Purpose: Users landing on the GitHub repo need a navigation hub that routes them to the right package quickly; the core package needs its own README so it ships standalone on npm with full API detail. Extracting API detail to `packages/core/README.md` gives API documentation a single source of truth and matches the structure of the existing adapter READMEs.

Output: Two markdown files — new hub-style root `README.md` and new `packages/core/README.md`.
</objective>

<execution_context>
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/workflows/execute-plan.md
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-docs-release-infra/08-CONTEXT.md
@.planning/phases/08-docs-release-infra/08-RESEARCH.md

# Source material for extraction — read in full before writing
@README.md
@packages/react/README.md
@packages/vue/README.md
@packages/svelte/README.md
@packages/core/package.json
</context>

<interfaces>
<!-- Existing adapter README structure (all three follow this shape). -->
<!-- packages/core/README.md must mirror it for consistency. -->

Section order used in packages/react/README.md, packages/vue/README.md, packages/svelte/README.md:
1. Title + one-line description with link back to repo root
2. Install (npm + pnpm commands)
3. Peer dependencies table (core README lists no peer deps — the core package has none, just browser native APIs)
4. Quick start (one minimal snippet)
5. Variant / usage examples
6. API reference (function signatures, parameters, options, return value)
7. TypeScript types re-exports
8. Browser support (link back to root README `#browser-support` anchor)
9. License

Package coordinates (from packages/core/package.json):
- name: `@ngockhoi96/ctc`
- version: `0.2.1`
- description: `Modular, tree-shakeable browser utilities library`
- exports subpaths: `.` and `./clipboard`
- no peer dependencies (zero runtime deps)

All four package README links from the root hub table (all relative):
- `./packages/core/README.md`
- `./packages/react/README.md`
- `./packages/vue/README.md`
- `./packages/svelte/README.md`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/core/README.md by extracting API reference from current root README</name>
  <files>packages/core/README.md</files>
  <read_first>
    - README.md (the full current root README — this is the source content to extract)
    - packages/react/README.md (section order template to follow)
    - packages/core/package.json (package name, description, version)
    - .planning/phases/08-docs-release-infra/08-CONTEXT.md (D-03, D-02 item 5 — browser support table stays at root)
    - .planning/phases/08-docs-release-infra/08-RESEARCH.md lines 449-467 (`packages/core/README.md` Extraction Plan mapping)
  </read_first>
  <action>
Create a new file at `packages/core/README.md` with exactly this section structure, in order, matching the adapter README shape:

1. **Title + one-liner** — `# @ngockhoi96/ctc` as H1, followed by one paragraph: "Core clipboard utilities for the [`@ngockhoi96/ctc`](https://github.com/anIcedAntFA/ctc) library. Zero runtime dependencies, tree-shakeable, SSR-safe, framework-agnostic." Then a blockquote with the existing SSR-safe note: `> All functions are SSR-safe -- safe to import in Next.js, Nuxt, or any server-side environment without crashing.`

2. **`## Install`** — copy the three install commands (`pnpm add @ngockhoi96/ctc`, `npm install @ngockhoi96/ctc`, `yarn add @ngockhoi96/ctc`) exactly as they appear in the current root README Installation section.

3. **`## Quick Start`** — copy the Quick Start code block from the current root README verbatim (the `copyToClipboard` + `isClipboardSupported` example).

4. **`## API Reference`** — copy the entire API Reference section from the current root README verbatim, including all five subsections (`### copyToClipboard`, `### readFromClipboard`, `### isClipboardSupported`, `### isClipboardReadSupported`, `### copyToClipboardLegacy`) with every code block, "Returns", "Browser support", and "Remarks" bullet intact. Do NOT paraphrase. Do NOT remove the `---` separators between functions.

5. **`## Error Handling`** — copy the full Error Handling section from the current root README verbatim: the `BrowserUtilsError` interface, `ClipboardOptions` interface, the Error Codes table, the "Using `onError`" example, and the closing paragraph about `console.warn` / `console.error` logging.

6. **`## Browser Support`** — per Claude's discretion decision documented in research (Pattern 3, recommendation): DO NOT duplicate the full browser-support table. Instead add this exact paragraph:
   ```
   See the [browser support table in the root README](../../README.md#browser-support). All functions require ES2020+ (>95% global browser support) and are SSR-safe.
   ```

7. **`## License`** — one line: `[MIT](../../LICENSE)`

Do NOT include: badges (stay at root), Contributing section (moves to CONTRIBUTING.md in Plan 02), Publishing section (moves to CONTRIBUTING.md), Versioning section (moves to CONTRIBUTING.md).

Use the same markdown code fences, heading levels, and formatting style as the source root README. Extraction means copy-paste, not rewrite.
  </action>
  <verify>
    <automated>test -f packages/core/README.md &amp;&amp; grep -q "^# @ngockhoi96/ctc$" packages/core/README.md &amp;&amp; grep -q "^## Install$" packages/core/README.md &amp;&amp; grep -q "^## Quick Start$" packages/core/README.md &amp;&amp; grep -q "^## API Reference$" packages/core/README.md &amp;&amp; grep -q "^### \`copyToClipboard" packages/core/README.md &amp;&amp; grep -q "^### \`readFromClipboard" packages/core/README.md &amp;&amp; grep -q "^### \`isClipboardSupported" packages/core/README.md &amp;&amp; grep -q "^### \`isClipboardReadSupported" packages/core/README.md &amp;&amp; grep -q "^### \`copyToClipboardLegacy" packages/core/README.md &amp;&amp; grep -q "^## Error Handling$" packages/core/README.md &amp;&amp; grep -q "BrowserUtilsError" packages/core/README.md &amp;&amp; grep -q "CLIPBOARD_NOT_SUPPORTED" packages/core/README.md &amp;&amp; grep -q "INSECURE_CONTEXT" packages/core/README.md &amp;&amp; grep -q "CLIPBOARD_PERMISSION_DENIED" packages/core/README.md &amp;&amp; grep -q "^## Browser Support$" packages/core/README.md &amp;&amp; grep -q "\\.\\./\\.\\./README\\.md#browser-support" packages/core/README.md &amp;&amp; grep -q "^## License$" packages/core/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/README.md` exists
    - Contains H1 `# @ngockhoi96/ctc` (exact match)
    - Contains `## Install`, `## Quick Start`, `## API Reference`, `## Error Handling`, `## Browser Support`, `## License` H2 sections in that order
    - All five function subsections present (`### \`copyToClipboard`, `### \`readFromClipboard`, `### \`isClipboardSupported`, `### \`isClipboardReadSupported`, `### \`copyToClipboardLegacy`)
    - All five error codes appear: `CLIPBOARD_NOT_SUPPORTED`, `INSECURE_CONTEXT`, `CLIPBOARD_PERMISSION_DENIED`, `CLIPBOARD_WRITE_FAILED`, `CLIPBOARD_READ_FAILED`
    - `BrowserUtilsError` interface appears in a code block
    - `ClipboardOptions` interface appears in a code block
    - Browser Support section contains the exact link `../../README.md#browser-support`
    - License section links to `../../LICENSE`
    - File does NOT contain a Contributing section, Publishing section, or Versioning section (grep -qv `^## Contributing`, `^## Publishing`, `^## Versioning`)
    - No badges (no `img.shields.io` URLs)
  </acceptance_criteria>
  <done>
    New `packages/core/README.md` exists with the full API reference content extracted verbatim from the current root README, following the same section structure as the adapter READMEs (react/vue/svelte).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite root README.md as a monorepo hub/landing page</name>
  <files>README.md</files>
  <read_first>
    - README.md (current single-package README — source for badges, SSR note, browser support table to keep)
    - packages/core/README.md (created in Task 1 — root quick-start must stay in sync with core API)
    - .planning/phases/08-docs-release-infra/08-CONTEXT.md (D-01, D-02 — locked section order)
    - .planning/phases/08-docs-release-infra/08-RESEARCH.md lines 449-467 (extraction mapping: what stays at root vs moves to core)
    - packages/core/package.json, packages/react/package.json, packages/vue/package.json, packages/svelte/package.json (names and descriptions for the package table)
  </read_first>
  <action>
Completely rewrite `README.md` as a hub/landing page with EXACTLY these sections in this order (D-02 locks this):

**Section 1 — Title + one-liner + badges.** Keep:
- H1: `# @ngockhoi96/ctc`
- One-paragraph description: `Modular, tree-shakeable browser utilities monorepo. Core clipboard APIs plus idiomatic framework adapters for React, Vue, and Svelte. Zero runtime dependencies, SSR-safe, framework-agnostic.`
- All four existing badges verbatim (npm version, bundle size, CI, license) — copy the exact markdown from the current README lines 5-8.
- Keep the SSR-safe blockquote: `> All functions are SSR-safe -- safe to import in Next.js, Nuxt, or any server-side environment without crashing.`

**Section 2 — `## Packages` table.** Create a markdown table with four rows (one per package):

```markdown
| Package | Description | Install |
|---------|-------------|---------|
| [`@ngockhoi96/ctc`](./packages/core/README.md) | Core clipboard utilities (copy, read, detect) | `pnpm add @ngockhoi96/ctc` |
| [`@ngockhoi96/ctc-react`](./packages/react/README.md) | React hook `useCopyToClipboard` | `pnpm add @ngockhoi96/ctc-react @ngockhoi96/ctc` |
| [`@ngockhoi96/ctc-vue`](./packages/vue/README.md) | Vue 3 composable `useCopyToClipboard` | `pnpm add @ngockhoi96/ctc-vue @ngockhoi96/ctc` |
| [`@ngockhoi96/ctc-svelte`](./packages/svelte/README.md) | Svelte action + rune/store | `pnpm add @ngockhoi96/ctc-svelte @ngockhoi96/ctc` |
```

**Section 3 — `## Quick Start`.** ONE minimal snippet — no API reference. Use this exact block:

~~~markdown
```typescript
import { copyToClipboard, isClipboardSupported } from '@ngockhoi96/ctc/clipboard'

button.addEventListener('click', async () => {
  if (isClipboardSupported()) {
    const success = await copyToClipboard('Hello, world!')
    console.log(success ? 'Copied!' : 'Copy failed')
  }
})
```

For framework-idiomatic usage, see the React, Vue, or Svelte package README linked above.
For the full API reference (all five functions, error codes, options), see [`packages/core/README.md`](./packages/core/README.md).
~~~

**Section 4 — `## Monorepo Structure`.** Short directory tree (depth 2, Claude's discretion per D-02 item 4):

~~~markdown
```
.
├── packages/
│   ├── core/      # @ngockhoi96/ctc — clipboard utilities
│   ├── react/     # @ngockhoi96/ctc-react — React hook
│   ├── vue/       # @ngockhoi96/ctc-vue — Vue composable
│   └── svelte/    # @ngockhoi96/ctc-svelte — action + rune
├── playground/    # standalone Vite apps per framework
└── .changeset/    # versioning + changelog config
```

Built with [pnpm workspaces](https://pnpm.io/workspaces) + [Turborepo](https://turbo.build/). See [CONTRIBUTING.md](./CONTRIBUTING.md) for setup.
~~~

**Section 5 — `## Browser Support`.** Keep the existing browser-support table from current README verbatim (lines ~250-260 in current file — the table with Function / Chrome / Firefox / Safari / HTTP Support columns). Keep the two paragraphs that follow it about ES2020+ and SSR-safe behaviour. The anchor `#browser-support` must be reachable via the H2 slug.

**Section 6 — `## License`.** One line: `[MIT](./LICENSE)`

**DELETE from root (now lives in `packages/core/README.md` — Task 1):**
- Installation section
- API Reference section (all five function subsections)
- Error Handling section (BrowserUtilsError, ClipboardOptions, Error Codes table, onError example)

**DELETE from root (now lives in CONTRIBUTING.md — Plan 02):**
- Contributing section (all of it)
- Development subsection
- Commit Format subsection
- Versioning subsection
- Publishing subsection

The final root README should be approximately 60-80 lines total (down from current 302).
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; grep -q "^# @ngockhoi96/ctc$" README.md &amp;&amp; grep -q "^## Packages$" README.md &amp;&amp; grep -q "^## Quick Start$" README.md &amp;&amp; grep -q "^## Monorepo Structure$" README.md &amp;&amp; grep -q "^## Browser Support$" README.md &amp;&amp; grep -q "^## License$" README.md &amp;&amp; grep -q "\\./packages/core/README\\.md" README.md &amp;&amp; grep -q "\\./packages/react/README\\.md" README.md &amp;&amp; grep -q "\\./packages/vue/README\\.md" README.md &amp;&amp; grep -q "\\./packages/svelte/README\\.md" README.md &amp;&amp; grep -q "img.shields.io/npm/v/@ngockhoi96/ctc" README.md &amp;&amp; grep -q "Bundle Size" README.md &amp;&amp; grep -q "ci.yml/badge.svg" README.md &amp;&amp; grep -q "License: MIT" README.md &amp;&amp; ! grep -q "^## API Reference$" README.md &amp;&amp; ! grep -q "^## Error Handling$" README.md &amp;&amp; ! grep -q "^## Contributing$" README.md &amp;&amp; ! grep -q "^## Publishing$" README.md &amp;&amp; ! grep -q "^## Versioning$" README.md &amp;&amp; ! grep -q "^## Installation$" README.md &amp;&amp; ! grep -q "BrowserUtilsError" README.md</automated>
  </verify>
  <acceptance_criteria>
    - Root `README.md` exists
    - Contains H2 sections in order: `## Packages`, `## Quick Start`, `## Monorepo Structure`, `## Browser Support`, `## License`
    - Package table contains exactly four package links: `./packages/core/README.md`, `./packages/react/README.md`, `./packages/vue/README.md`, `./packages/svelte/README.md`
    - All four original badges present (npm version, bundlephobia, CI, MIT license)
    - SSR-safe blockquote retained
    - Browser Support table retained (row for `copyToClipboard`, `readFromClipboard`, etc.)
    - Does NOT contain H2 `## API Reference` (moved to packages/core)
    - Does NOT contain H2 `## Error Handling` (moved to packages/core)
    - Does NOT contain H2 `## Contributing`, `## Publishing`, or `## Versioning` (moving to CONTRIBUTING.md in Plan 02)
    - Does NOT contain H2 `## Installation` (replaced by package table)
    - Does NOT reference `BrowserUtilsError` (per D-01 — detail lives in core README)
    - Word "Packages" appears as an H2 heading
    - Quick Start has exactly one code fence (minimal snippet per D-02 item 3)
  </acceptance_criteria>
  <done>
    Root `README.md` is now a hub/landing page per D-01/D-02: title + badges + package table + one-snippet quick-start + monorepo tree + browser support + license. All API detail has been removed (relocated to `packages/core/README.md`). All contributing/publishing/versioning content has been removed (will move to CONTRIBUTING.md in Plan 02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public npm/GitHub readers | README content is rendered publicly on GitHub + npm package pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | README.md / packages/core/README.md | mitigate | Do not paste secrets, private URLs, or internal infra references. Only public repo URL `https://github.com/anIcedAntFA/ctc` is included. |
| T-08-02 | Tampering (malicious link injection) | README.md package table | mitigate | All links are relative paths (`./packages/*/README.md`) — no external redirects or shortened URLs. Badge URLs reuse the exact values already in current README. |
| T-08-03 | Spoofing (typosquat package names) | README.md package table | mitigate | All four package names copied verbatim from the authoritative `packages/*/package.json` `name` fields — no free-form typing of package names. |

Docs-only changes — no code execution surface. ASVS L1 not applicable beyond V14 Configuration (covered in Plan 03 for `.changeset/config.json`).
</threat_model>

<verification>
After both tasks complete:
1. `test -f README.md && test -f packages/core/README.md` — both files exist
2. Open `README.md` and confirm visual section order matches D-02
3. Open `packages/core/README.md` and confirm all five function signatures render in GitHub preview
4. `grep -c "packages/core/README.md" README.md` returns at least 2 (package table row + Quick Start pointer)
5. Link the browser support anchor from core README: navigate `packages/core/README.md` → `../../README.md#browser-support` resolves
6. Run `pnpm lint` — must still pass (READMEs aren't linted but shouldn't break any other check)
</verification>

<success_criteria>
- [ ] DX-05 satisfied: Root README reflects monorepo structure with links to all 4 packages
- [ ] DX-06 satisfied (core portion): `packages/core/README.md` exists with full API reference
- [ ] All four package links in root table are relative and resolve
- [ ] Root README ≤ 100 lines (was 302, target is hub/landing page)
- [ ] `packages/core/README.md` contains all 5 function signatures and all 5 error codes verbatim
- [ ] No duplicated content between root README and `packages/core/README.md` (API detail only in core)
- [ ] All existing badges retained at root
- [ ] Browser support table still at root (D-02 item 5)
</success_criteria>

<output>
After completion, create `.planning/phases/08-docs-release-infra/08-01-SUMMARY.md`
</output>
