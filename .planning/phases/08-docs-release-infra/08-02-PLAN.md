---
phase: 08-docs-release-infra
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - SECURITY.md
  - .github/PULL_REQUEST_TEMPLATE.md
  - .github/ISSUE_TEMPLATE/bug_report.md
  - .github/ISSUE_TEMPLATE/feature_request.md
  - .github/ISSUE_TEMPLATE/config.yml
autonomous: true
requirements:
  - DX-07
  - DX-08
  - DX-09
user_setup: []
must_haves:
  truths:
    - "A contributor cloning the repo can follow CONTRIBUTING.md step-by-step to install, run tests, create a changeset, and ship a release"
    - "A security researcher discovering a vulnerability is routed to GitHub private vulnerability reporting via SECURITY.md, not to a public issue"
    - "A contributor opening a PR on GitHub sees the PR template prompting them for Summary, Type of change, Test plan, and a changeset reminder"
    - "A contributor opening a new issue on GitHub sees a chooser with Bug report and Feature request templates (blank issues disabled)"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Lean ~150-line monorepo contributor guide with six required sections"
      contains: "## Release flow"
    - path: "SECURITY.md"
      provides: "Vulnerability reporting policy routed to GitHub private vulnerability reporting"
      contains: "## Reporting a Vulnerability"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR template with Summary / Type of change / Test plan / changeset reminder"
      contains: "## Type of change"
    - path: ".github/ISSUE_TEMPLATE/bug_report.md"
      provides: "Bug report issue template with frontmatter"
      contains: "name: Bug report"
    - path: ".github/ISSUE_TEMPLATE/feature_request.md"
      provides: "Feature request issue template with frontmatter"
      contains: "name: Feature request"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "Disables blank issues (user-approved addition)"
      contains: "blank_issues_enabled: false"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "SECURITY.md"
      via: "markdown link in Security section"
      pattern: "\\./SECURITY\\.md"
    - from: "SECURITY.md"
      to: "GitHub Security tab (private vulnerability reporting)"
      via: "markdown link to repo /security/advisories/new"
      pattern: "github\\.com/anIcedAntFA/ctc/security"
---

<objective>
Create the repo housekeeping files: `CONTRIBUTING.md`, `SECURITY.md`, the GitHub PR template, and two markdown issue templates (bug report, feature request) plus a `config.yml` that disables blank issues. This closes DX-07 (contributing guide), DX-08 (security policy), and DX-09 (PR + issue templates).

Purpose: External contributors need a clear path from clone to release; security researchers need a documented private reporting channel; GitHub's PR/issue chooser needs templates so maintainers receive structured bug reports and feature requests instead of free-form text.

Output: Six new files (one CONTRIBUTING, one SECURITY, one PR template, two issue templates, one config).
</objective>

<execution_context>
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/workflows/execute-plan.md
@/home/ngockhoi96/workspace/github.com/anIcedAntFA/ctc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-docs-release-infra/08-CONTEXT.md
@.planning/phases/08-docs-release-infra/08-RESEARCH.md

# Existing commands referenced by CONTRIBUTING.md
@CLAUDE.md
@package.json
@pnpm-workspace.yaml
@turbo.json
</context>

<interfaces>
<!-- Commands CONTRIBUTING.md must reference (from root package.json and CLAUDE.md). -->

Root package.json scripts (already exist):
- `pnpm install` — installs dependencies
- `pnpm setup` — installs lefthook git hooks
- `pnpm lint` — `turbo run lint` (biome across all packages)
- `pnpm test` — `turbo run test` (vitest across all packages)
- `pnpm test:e2e` — `turbo run test:e2e` (playwright in packages/core)
- `pnpm build` — `turbo run build` (tsdown across all packages)
- `pnpm changeset` — runs changesets CLI
- `pnpm validate` — `turbo run validate` (publint + attw)

Per-package filter command pattern: `pnpm --filter @ngockhoi96/ctc-<name> <script>`

Conventional commit format (from commitlint config): `feat|fix|chore|docs|test|ci(scope): description`

Repository URL: `https://github.com/anIcedAntFA/ctc`

GitHub private vulnerability reporting URL (for this repo):
`https://github.com/anIcedAntFA/ctc/security/advisories/new`

Changeset emoji prefixes (configured in Plan 03 — CONTRIBUTING.md should reference them):
- 💥 major
- ✨ minor
- 🐞 patch
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create CONTRIBUTING.md (lean monorepo contributor guide)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CLAUDE.md (commands, commit format, code style rules the guide must reference)
    - package.json (root scripts that CONTRIBUTING.md documents)
    - .planning/phases/08-docs-release-infra/08-CONTEXT.md (D-04 six required sections, D-05 release flow detail)
    - .planning/phases/08-docs-release-infra/08-RESEARCH.md lines 469-548 (CONTRIBUTING.md skeleton)
  </read_first>
  <action>
Create `CONTRIBUTING.md` at the repo root with the following content. Target: ~100-150 lines. The file MUST contain the six sections D-04 requires, in this order.

Use this exact content as the starting point (adapt wording where noted but preserve section headings exactly):

```markdown
# Contributing

Thanks for your interest in `@ngockhoi96/ctc`! This repo is a pnpm + Turborepo monorepo
housing the core clipboard library and its framework adapters. Below is everything you
need to set up, run tests, and ship a change.

## Prerequisites

- **Node.js** >= 20 (see `.nvmrc` if present)
- **pnpm** >= 10 (`npm i -g pnpm`)
- **Git**

## Setup

```bash
git clone https://github.com/anIcedAntFA/ctc.git
cd ctc
pnpm install
pnpm setup    # installs lefthook git hooks (pre-commit + commit-msg)
```

After `pnpm setup`, every commit runs `pnpm lint` and `pnpm test` via lefthook, and
commit messages are validated by commitlint.

## Running tests

From the repo root, all tasks are orchestrated by Turborepo:

```bash
pnpm lint          # biome check across all packages
pnpm test          # vitest unit tests across all packages
pnpm test:e2e      # playwright tests (packages/core)
pnpm build         # tsdown builds across all packages
pnpm validate      # publint + arethetypeswrong for all publishable packages
```

To run a task for a single package, use a filter:

```bash
pnpm --filter @ngockhoi96/ctc-react test
pnpm --filter @ngockhoi96/ctc-vue test
pnpm --filter @ngockhoi96/ctc-svelte test
pnpm --filter @ngockhoi96/ctc test:e2e
```

## Adding a new package

1. Create `packages/<name>/` with `package.json`, `tsconfig.json`, and `tsdown.config.ts`.
   Copy the shape from an existing adapter (e.g. `packages/react/`) and adjust the
   `name`, `description`, `peerDependencies`, and entry points.
2. Workspaces are auto-discovered via `pnpm-workspace.yaml` (`packages/*`) — no manual
   registration needed.
3. Add `build`, `lint`, `test`, `typecheck`, `validate` scripts so the Turborepo
   pipeline picks them up. See `turbo.json` for the task graph.
4. Add a `packages/<name>/README.md` following the structure of existing package
   READMEs (Title → Install → Peer deps → Quick start → API → Browser support → License).
5. If the package is publishable, add it to `.changeset/config.json` workspace
   discovery (automatic for anything under `packages/`).

## Creating a changeset

Every PR that changes public API or observable behaviour needs a changeset:

```bash
pnpm changeset
```

The CLI prompts you to:
1. Select which packages are affected
2. Pick a bump type per package:
   - **patch** — bug fix, no API change
   - **minor** — new feature, backwards compatible
   - **major** — breaking change
3. Write a one-line summary — this becomes the `CHANGELOG.md` entry.

The custom changelog formatter automatically prepends an emoji based on bump type:

| Bump type | Emoji |
|-----------|-------|
| major     | 💥    |
| minor     | ✨    |
| patch     | 🐞    |

Commit the generated `.changeset/*.md` file alongside your code changes.

## Release flow

This repo uses [changesets](https://github.com/changesets/changesets) in **independent
mode** — each package versions and publishes separately.

The full flow:

1. **You open a PR** with code changes plus a changeset. CI runs `lint`, `test`,
   `test:e2e`, `build`, and `validate`.
2. **After merge to `master`**, the `changesets/action` bot opens (or updates) a
   `Version Packages` PR. This PR:
   - Consumes every pending changeset under `.changeset/`
   - Bumps the affected packages' versions in their `package.json`
   - Regenerates each package's `CHANGELOG.md` with the emoji-prefixed entries
3. **Merging the Version PR** triggers `pnpm changeset publish` in CI, which:
   - Publishes bumped packages to npm (using `NPM_TOKEN` secret)
   - Creates a GitHub Release per published package with the changelog entries
4. **Independent mode note:** a single Version PR can bump multiple packages at
   different levels — for example `@ngockhoi96/ctc@0.3.0` (minor) alongside
   `@ngockhoi96/ctc-react@0.2.1` (patch). Packages are never locked in step.

When in doubt about bump type, default to **patch** for any code change and **minor**
for any new exported function or option. Reserve **major** for removals or
signature-breaking changes.

## Code style

- **TypeScript strict** — no `any`, no `as` casts unless documented
- **Named exports only** — no default exports (enforced by Biome)
- **Biome** handles lint + format: `pnpm lint:fix` auto-fixes what it can
- **Zero runtime dependencies** in published packages — only browser native APIs
- **Conventional commits** enforced by commitlint:

  ```
  feat(clipboard): add new utility function
  fix(clipboard): handle edge case in Safari
  chore: update dependencies
  docs(readme): clarify install instructions
  ```

  Types: `feat`, `fix`, `chore`, `docs`, `test`, `ci`, `refactor`, `perf`.

## Security

For vulnerability reports, see [SECURITY.md](./SECURITY.md). **Do not open a public
issue for security concerns** — use GitHub's private vulnerability reporting.

## Questions

Open a [GitHub Discussion](https://github.com/anIcedAntFA/ctc/discussions) for general
questions, or a bug/feature issue via the templates in the "New issue" chooser.
```

Adjust line counts as needed but all six D-04 sections MUST be present. Do NOT add sections beyond the six (plus the introductory paragraph, Code style, Security, Questions — these are additive per the research skeleton and still keep the file under ~150 lines).
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md &amp;&amp; grep -q "^## Prerequisites$" CONTRIBUTING.md &amp;&amp; grep -q "^## Setup$" CONTRIBUTING.md &amp;&amp; grep -q "^## Running tests$" CONTRIBUTING.md &amp;&amp; grep -q "^## Adding a new package$" CONTRIBUTING.md &amp;&amp; grep -q "^## Creating a changeset$" CONTRIBUTING.md &amp;&amp; grep -q "^## Release flow$" CONTRIBUTING.md &amp;&amp; grep -q "pnpm install" CONTRIBUTING.md &amp;&amp; grep -q "pnpm setup" CONTRIBUTING.md &amp;&amp; grep -q "pnpm test:e2e" CONTRIBUTING.md &amp;&amp; grep -q "pnpm changeset" CONTRIBUTING.md &amp;&amp; grep -q "independent mode" CONTRIBUTING.md &amp;&amp; grep -q "Version Packages" CONTRIBUTING.md &amp;&amp; grep -q "changeset publish" CONTRIBUTING.md &amp;&amp; grep -q "💥" CONTRIBUTING.md &amp;&amp; grep -q "✨" CONTRIBUTING.md &amp;&amp; grep -q "🐞" CONTRIBUTING.md &amp;&amp; grep -q "\\./SECURITY\\.md" CONTRIBUTING.md &amp;&amp; [ $(wc -l &lt; CONTRIBUTING.md) -lt 200 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `CONTRIBUTING.md` exists at repo root
    - Contains all six required H2 sections: `## Prerequisites`, `## Setup`, `## Running tests`, `## Adding a new package`, `## Creating a changeset`, `## Release flow`
    - References commands: `pnpm install`, `pnpm setup`, `pnpm test`, `pnpm test:e2e`, `pnpm build`, `pnpm changeset`
    - Shows per-package filter example with `pnpm --filter @ngockhoi96/ctc-react test`
    - Release flow section explicitly covers: when to create a changeset, bump types (patch/minor/major), Version PR behaviour, publish step, and independent-mode note
    - Contains all three emoji: 💥 ✨ 🐞 (mapped to bump types)
    - Links to `./SECURITY.md`
    - Mentions conventional commit format with prefixes `feat`, `fix`, `chore`, `docs`, `test`, `ci`
    - Total line count < 200 (target ~100-150, cap at 200 allows for generous formatting)
  </acceptance_criteria>
  <done>
    `CONTRIBUTING.md` exists at repo root with the six D-04 sections, references all real commands from root `package.json`, documents the D-05 release flow walkthrough, and stays under 200 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SECURITY.md routing to GitHub private vulnerability reporting</name>
  <files>SECURITY.md</files>
  <read_first>
    - .planning/phases/08-docs-release-infra/08-RESEARCH.md lines 550-584 (SECURITY.md template + planner action note on GitHub private reporting)
    - The user decision in this phase's planning_context: "SECURITY.md: Use GitHub private vulnerability reporting (NOT an email address). The SECURITY.md links to the repo's Security tab."
  </read_first>
  <action>
Create `SECURITY.md` at the repo root using GitHub's private vulnerability reporting as the intake channel (user override of the research template — NO email address). Exact content:

```markdown
# Security Policy

## Supported Versions

Only the latest published version of each package receives security updates. Older
versions are not patched.

| Package                  | Supported |
|--------------------------|-----------|
| `@ngockhoi96/ctc`        | ✅ latest |
| `@ngockhoi96/ctc-react`  | ✅ latest |
| `@ngockhoi96/ctc-vue`    | ✅ latest |
| `@ngockhoi96/ctc-svelte` | ✅ latest |

## Reporting a Vulnerability

**Please do NOT open a public GitHub issue for security reports.**

This repository uses GitHub's private vulnerability reporting. To report a security
issue:

1. Go to the [Security tab](https://github.com/anIcedAntFA/ctc/security) of this
   repository.
2. Click **Report a vulnerability** (or use the direct link:
   [Report a vulnerability](https://github.com/anIcedAntFA/ctc/security/advisories/new)).
3. Fill in the advisory form with:
   - A clear description of the vulnerability
   - Steps to reproduce
   - Affected package(s) and version(s)
   - A proof-of-concept (if possible)
   - A suggested fix (if you have one)

## What to expect

- You will receive an acknowledgment within **72 hours** of submission.
- The maintainer will investigate and confirm the vulnerability privately.
- Fixes are developed, reviewed, and released under a GitHub Security Advisory before
  public disclosure.
- Once a patch ships, the advisory is published and credit is given to the reporter
  (unless anonymity is requested).

## Scope

This policy covers the published npm packages in this repository:
`@ngockhoi96/ctc`, `@ngockhoi96/ctc-react`, `@ngockhoi96/ctc-vue`, and
`@ngockhoi96/ctc-svelte`. It does not cover the playgrounds (`playground/*`), which
are demo apps and not published to npm.
```

Do NOT include any email address — the user explicitly decided to use GitHub private vulnerability reporting only.
  </action>
  <verify>
    <automated>test -f SECURITY.md &amp;&amp; grep -q "^# Security Policy$" SECURITY.md &amp;&amp; grep -q "^## Supported Versions$" SECURITY.md &amp;&amp; grep -q "^## Reporting a Vulnerability$" SECURITY.md &amp;&amp; grep -q "github\\.com/anIcedAntFA/ctc/security" SECURITY.md &amp;&amp; grep -q "github\\.com/anIcedAntFA/ctc/security/advisories/new" SECURITY.md &amp;&amp; grep -q "private vulnerability reporting" SECURITY.md &amp;&amp; grep -q "72 hours" SECURITY.md &amp;&amp; grep -q "@ngockhoi96/ctc-react" SECURITY.md &amp;&amp; grep -q "@ngockhoi96/ctc-vue" SECURITY.md &amp;&amp; grep -q "@ngockhoi96/ctc-svelte" SECURITY.md &amp;&amp; ! grep -qiE "email|mailto:|@[a-z0-9.-]+\\.[a-z]{2,}" SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `SECURITY.md` exists at repo root
    - Contains H2 sections: `## Supported Versions`, `## Reporting a Vulnerability`
    - Links to `https://github.com/anIcedAntFA/ctc/security` (Security tab)
    - Links to `https://github.com/anIcedAntFA/ctc/security/advisories/new` (new advisory form)
    - Uses the phrase "private vulnerability reporting"
    - Mentions a 72-hour acknowledgment timeline
    - Lists all four packages in the Supported Versions table
    - Does NOT contain any email address (no `mailto:`, no `@`-shaped email pattern like `foo@bar.com`) — user decision locks this
    - Does NOT use the word "email" or "Email" as a reporting channel
  </acceptance_criteria>
  <done>
    `SECURITY.md` exists, routes vulnerability reports to GitHub's private vulnerability reporting (per user decision), lists all four packages, and contains no email contact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub PR template + two issue templates + config.yml</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md, .github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/feature_request.md, .github/ISSUE_TEMPLATE/config.yml</files>
  <read_first>
    - .planning/phases/08-docs-release-infra/08-CONTEXT.md (D-10 PR template content, D-11 issue template content)
    - .planning/phases/08-docs-release-infra/08-RESEARCH.md lines 241-266 (GitHub discovery rules + frontmatter requirement), lines 387-425 (bug report template + optional config.yml)
    - User decision in planning_context: "config.yml: Include it (6 lines, disables blank issues)."
  </read_first>
  <action>
Create four files. `.github/ISSUE_TEMPLATE/` directory already needs to exist — create it if not present. (The `.github/workflows/` directory already exists, so `.github/` is present.)

**IMPORTANT — placeholder convention:** The token `___YAML_FENCE___` appearing in the code blocks below is a stand-in for the literal three-hyphen YAML frontmatter delimiter (`---`). When you write the actual template files to disk, replace EVERY occurrence of `___YAML_FENCE___` with exactly `---` (three hyphens on their own line). This substitution is necessary only because this planner file is itself parsed as YAML frontmatter and cannot contain nested `---` blocks inside code fences. The final files on disk MUST use `---` — GitHub will not register the issue templates otherwise.

**File 1: `.github/PULL_REQUEST_TEMPLATE.md`** (no frontmatter required for PR templates):

```markdown
## Summary

<!-- Bullet points describing what this PR does and why. -->

-
-

## Type of change

- [ ] Bug fix (non-breaking change that fixes an issue)
- [ ] New feature (non-breaking change that adds functionality)
- [ ] Breaking change (fix or feature causing existing functionality to change)
- [ ] Documentation update

## Test plan

<!-- How did you verify this works? Check all that apply and add specifics. -->

- [ ] `pnpm lint` passes
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e` passes (if core clipboard logic changed)
- [ ] `pnpm build` passes
- [ ] Manual verification steps:
  -

## Changeset

> [!IMPORTANT]
> If this PR introduces a **breaking change** or a **new feature**, run `pnpm changeset`
> and commit the generated file. See [CONTRIBUTING.md](./CONTRIBUTING.md#creating-a-changeset)
> for bump-type guidance.
```

**File 2: `.github/ISSUE_TEMPLATE/bug_report.md`** (MUST include YAML frontmatter so GitHub registers it in the "New issue" chooser):

```markdown
___YAML_FENCE___
name: Bug report
about: Report a reproducible bug in the library
title: '[Bug] '
labels: bug
assignees: ''
___YAML_FENCE___

## Describe the bug

A clear and concise description of the bug.

## Steps to reproduce

1.
2.
3.

## Expected behavior

What you expected to happen.

## Actual behavior

What actually happened. Include error messages, stack traces, or console output.

## Environment

- **OS:** (e.g. macOS 14.5, Windows 11, Ubuntu 22.04)
- **Browser + version:** (e.g. Chrome 130, Firefox 131, Safari 17.6)
- **Package + version:** (e.g. `@ngockhoi96/ctc@0.2.1`, `@ngockhoi96/ctc-react@0.1.0`)
- **Framework + version (if applicable):** (e.g. React 18.3, Vue 3.5, Svelte 5.0)
- **Node version (if SSR):**

## Minimal reproduction

<!-- A link to a StackBlitz/CodeSandbox, or a minimal code snippet that reproduces the issue. -->

## Additional context

<!-- Any other context about the problem: screenshots, related issues, workarounds tried. -->
```

**File 3: `.github/ISSUE_TEMPLATE/feature_request.md`** (MUST include YAML frontmatter):

```markdown
___YAML_FENCE___
name: Feature request
about: Suggest a new feature or improvement
title: '[Feature] '
labels: enhancement
assignees: ''
___YAML_FENCE___

## Problem to solve

What problem are you trying to solve? What use case isn't covered today?

## Proposed solution

How do you imagine this working? Include API sketches, code examples, or
configuration ideas if you have them.

## Alternatives considered

What other approaches did you consider? Why didn't they work?

## Additional context

<!-- Related issues, links to prior art in other libraries, or anything else that helps. -->
```

**File 4: `.github/ISSUE_TEMPLATE/config.yml`** (per user decision — include this file to disable blank issues):

```yaml
blank_issues_enabled: false
contact_links:
  - name: Questions & Discussion
    url: https://github.com/anIcedAntFA/ctc/discussions
    about: For general questions or usage help, open a Discussion instead of an issue.
```

All three issue-template markdown files MUST start with the `---` frontmatter block — without it, GitHub silently ignores them and shows a blank issue form.
  </action>
  <verify>
    <automated>test -f .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; test -f .github/ISSUE_TEMPLATE/bug_report.md &amp;&amp; test -f .github/ISSUE_TEMPLATE/feature_request.md &amp;&amp; test -f .github/ISSUE_TEMPLATE/config.yml &amp;&amp; grep -q "^## Summary$" .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -q "^## Type of change$" .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -q "^## Test plan$" .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -q "pnpm changeset" .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -q "\\[ \\] Bug fix" .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -q "\\[ \\] New feature" .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -q "\\[ \\] Breaking change" .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -q "\\[ \\] Documentation update" .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; head -1 .github/ISSUE_TEMPLATE/bug_report.md | grep -q "^---$" &amp;&amp; grep -q "^name: Bug report$" .github/ISSUE_TEMPLATE/bug_report.md &amp;&amp; grep -q "^about:" .github/ISSUE_TEMPLATE/bug_report.md &amp;&amp; grep -q "^## Steps to reproduce$" .github/ISSUE_TEMPLATE/bug_report.md &amp;&amp; grep -q "^## Expected behavior$" .github/ISSUE_TEMPLATE/bug_report.md &amp;&amp; grep -q "^## Actual behavior$" .github/ISSUE_TEMPLATE/bug_report.md &amp;&amp; grep -q "^## Environment$" .github/ISSUE_TEMPLATE/bug_report.md &amp;&amp; head -1 .github/ISSUE_TEMPLATE/feature_request.md | grep -q "^---$" &amp;&amp; grep -q "^name: Feature request$" .github/ISSUE_TEMPLATE/feature_request.md &amp;&amp; grep -q "^## Problem to solve$" .github/ISSUE_TEMPLATE/feature_request.md &amp;&amp; grep -q "^## Proposed solution$" .github/ISSUE_TEMPLATE/feature_request.md &amp;&amp; grep -q "^## Alternatives considered$" .github/ISSUE_TEMPLATE/feature_request.md &amp;&amp; grep -q "^blank_issues_enabled: false$" .github/ISSUE_TEMPLATE/config.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/PULL_REQUEST_TEMPLATE.md` exists with four H2 sections: Summary, Type of change, Test plan, (plus Changeset callout)
    - PR template has all four checkbox options: `Bug fix`, `New feature`, `Breaking change`, `Documentation update`
    - PR template references `pnpm changeset`
    - `.github/ISSUE_TEMPLATE/bug_report.md` exists
    - First line of bug_report.md is `---` (start of frontmatter block)
    - bug_report.md frontmatter contains `name: Bug report` and `about:` lines
    - bug_report.md body contains H2 sections: Describe the bug, Steps to reproduce, Expected behavior, Actual behavior, Environment
    - `.github/ISSUE_TEMPLATE/feature_request.md` exists
    - First line of feature_request.md is `---` (start of frontmatter block)
    - feature_request.md frontmatter contains `name: Feature request`
    - feature_request.md body contains H2 sections: Problem to solve, Proposed solution, Alternatives considered
    - `.github/ISSUE_TEMPLATE/config.yml` exists with `blank_issues_enabled: false` (exact string at line start)
    - config.yml contains a `contact_links:` section pointing to GitHub Discussions
  </acceptance_criteria>
  <done>
    All four GitHub template files exist under `.github/`. PR template satisfies D-10. Both issue templates have valid YAML frontmatter and satisfy D-11. `config.yml` disables blank issues per user decision.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External reporter → maintainer (SECURITY.md channel) | Vulnerability reports flow through GitHub private advisories, not public issues |
| External contributor → PR template | PR body text renders publicly on GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | SECURITY.md | mitigate | Use GitHub private vulnerability reporting (user decision). No email address, no contact name, no infra details published in SECURITY.md. Reporters file via `https://github.com/anIcedAntFA/ctc/security/advisories/new` which keeps the report private until a patch ships. |
| T-08-05 | Information Disclosure | CONTRIBUTING.md | mitigate | Document only public commands and public repo URL. Do not publish `NPM_TOKEN` handling details beyond "stored as a GitHub Actions secret". |
| T-08-06 | Spoofing | .github/ISSUE_TEMPLATE/config.yml | mitigate | `contact_links` only routes to the official Discussions URL under the `anIcedAntFA/ctc` repo — no third-party redirect. |
| T-08-07 | Tampering (malicious template content) | PR/issue templates | accept | Templates are static markdown rendered by GitHub; no code execution surface. Markdown checkboxes and comments cannot execute code. |

ASVS L1 V14 Configuration: the one executable YAML file (`config.yml`) uses only documented GitHub keys (`blank_issues_enabled`, `contact_links`) — no custom deserialization path.
</threat_model>

<verification>
After all three tasks complete:
1. `ls CONTRIBUTING.md SECURITY.md .github/PULL_REQUEST_TEMPLATE.md .github/ISSUE_TEMPLATE/bug_report.md .github/ISSUE_TEMPLATE/feature_request.md .github/ISSUE_TEMPLATE/config.yml` — all six exist
2. `head -1 .github/ISSUE_TEMPLATE/bug_report.md` returns `---` (frontmatter present — without this, GitHub ignores the template)
3. `head -1 .github/ISSUE_TEMPLATE/feature_request.md` returns `---`
4. `! grep -qE '@[a-z0-9.-]+\.[a-z]{2,}' SECURITY.md` — no email pattern present (user decision)
5. `wc -l CONTRIBUTING.md` returns a count < 200
6. `pnpm lint` still passes (no lint target covers these files, but the run should not error)
</verification>

<success_criteria>
- [ ] DX-07 satisfied: `CONTRIBUTING.md` covers all six D-04 sections including step-by-step release flow (D-05)
- [ ] DX-08 satisfied: `SECURITY.md` routes reports to GitHub private vulnerability reporting (no email)
- [ ] DX-09 satisfied: PR template + two issue templates (with frontmatter) + config.yml disabling blank issues
- [ ] All issue templates have valid YAML frontmatter (first line is `---`)
- [ ] No email addresses anywhere in SECURITY.md (user override of the research template)
- [ ] `config.yml` `blank_issues_enabled: false` prevents blank issues (user decision)
</success_criteria>

<output>
After completion, create `.planning/phases/08-docs-release-infra/08-02-SUMMARY.md`
</output>
